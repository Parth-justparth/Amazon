[project]
name = "secondlife-ai"
version = "0.1.0"
description = "Amazon SecondLife AI return-interception backend"
requires-python = ">=3.11"

[tool.pytest.ini_options]
testpaths = ["tests"]
python_files = ["test_*.py"]
python_classes = ["Test*"]
python_functions = ["test_*"]
addopts = "-ra"
